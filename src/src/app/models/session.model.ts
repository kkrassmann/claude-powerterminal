/**
 * Session metadata model — re-exported from shared for Angular imports.
 * Canonical definition lives in src/shared/session-types.ts.
 */
export { SessionMetadata } from '../../../shared/session-types';
