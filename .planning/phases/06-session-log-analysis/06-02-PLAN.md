---
phase: 06-session-log-analysis
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/src/app/services/log-analysis.service.ts
  - src/src/app/components/analysis-panel/analysis-panel.component.ts
  - src/src/app/components/analysis-panel/analysis-panel.component.html
  - src/src/app/components/analysis-panel/analysis-panel.component.css
  - src/src/app/components/tile-header/tile-header.component.ts
  - src/src/app/components/tile-header/tile-header.component.html
  - src/src/app/components/tile-header/tile-header.component.css
  - src/src/app/components/dashboard/dashboard.component.ts
  - src/src/app/app.component.ts
  - src/src/app/app.component.html
  - src/src/app/app.component.css
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - OPT-03

must_haves:
  truths:
    - "Angular service provides analysis data via BehaviorSubject with dual-mode (IPC/HTTP)"
    - "Analysis panel shows overview, tool usage bars, token usage, problems, recommendations"
    - "Tool usage rendered as pure CSS bars with Catppuccin colors (no chart library)"
    - "Recommendations show severity-colored cards (praise=green, info=blue, warning=peach, critical=red)"
    - "Tile headers display per-session practice score (0-100) and badge chips"
    - "Dashboard loads session scores and passes them to tile-header components"
    - "Analysis panel is toggled via a button in the app header"
    - "Remote browsers access analysis via HTTP API endpoints"
  artifacts:
    - path: "src/src/app/services/log-analysis.service.ts"
      provides: "Angular service for analysis data"
      exports: ["LogAnalysisService"]
    - path: "src/src/app/components/analysis-panel/analysis-panel.component.ts"
      provides: "Collapsible analysis panel UI"
      exports: ["AnalysisPanelComponent"]
  key_links:
    - from: "src/src/app/app.component.ts"
      to: "src/src/app/components/analysis-panel/analysis-panel.component.ts"
      via: "import and template usage"
      pattern: "AnalysisPanelComponent"
    - from: "src/src/app/components/dashboard/dashboard.component.ts"
      to: "src/src/app/services/log-analysis.service.ts"
      via: "inject and load session scores"
      pattern: "LogAnalysisService"
---

<objective>
Build the frontend UI: Angular analysis service, collapsible analysis panel with tool/token/recommendation sections, per-session practice score + badges in tile headers, dashboard wiring, and app integration.

Purpose: Display the analysis data from Plan 01's backend in the dashboard — a global analysis panel for deep insights, plus per-session scores and badges in each tile header for at-a-glance feedback.

Output: 1 new service, 1 new component (3 files), 4 modified components (tile-header, dashboard, app).
</objective>

<execution_context>
@C:/Users/Konstantin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Konstantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/shared/analysis-types.ts
@src/shared/ipc-channels.ts
@src/src/app/services/git-context.service.ts
@src/src/app/components/tile-header/tile-header.component.ts
@src/src/app/components/tile-header/tile-header.component.html
@src/src/app/components/tile-header/tile-header.component.css
@src/src/app/components/dashboard/dashboard.component.ts
@src/src/app/app.component.ts
@src/src/app/app.component.html
@src/src/app/app.component.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Angular log-analysis service</name>
  <files>
    src/src/app/services/log-analysis.service.ts
  </files>
  <action>
Create `src/src/app/services/log-analysis.service.ts`:

- Injectable, providedIn: 'root'
- BehaviorSubject pattern like GitContextService
- Dual-mode: IPC when `window.electronAPI`, HTTP fetch when remote browser
- `loadAnalysis()`: Fetches SessionAnalysis, emits via BehaviorSubject
- `loadSessionScore(sessionId)`: Fetches SessionPracticeScore for a single session
- `analysis$`: Observable for components to subscribe
- Loading state tracking
- Error handling with console.warn (silent failure like git service)
  </action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal/src && npx ng build --configuration production 2>&1 | tail -5</automated>
  </verify>
  <done>Service compiles and follows existing patterns</done>
</task>

<task type="auto">
  <name>Task 2: Build analysis panel component</name>
  <files>
    src/src/app/components/analysis-panel/analysis-panel.component.ts
    src/src/app/components/analysis-panel/analysis-panel.component.html
    src/src/app/components/analysis-panel/analysis-panel.component.css
  </files>
  <action>
Create analysis-panel component (3 files):

**TypeScript:**
- Standalone component, imports CommonModule
- Injects LogAnalysisService
- OnInit: calls loadAnalysis()
- Subscribes to analysis$ observable

**HTML — Sections:**
1. **Overview:** Sessions, Messages, Tool-Calls, Date range — simple stat row
2. **Tool Usage:** Horizontal CSS bars sorted by frequency, max bar = 100%, others proportional
3. **Token Usage:** Cache-Hit-Ratio as progress bar, total input/output/cache numbers
4. **Problems:** List with type badge and message, count indicator
5. **Recommendations:** Cards with severity color coding:
   - praise = green (#a6e3a1)
   - info = blue (#89b4fa)
   - warning = peach (#fab387)
   - critical = red (#f38ba8)
6. **Practice Score:** Overall score 0-100 with visual indicator

**CSS:**
- Catppuccin Mocha palette (matches existing app theme)
- Pure CSS bars (no chart library): `width: percentage%` with colored backgrounds
- Collapsible sections with toggle
- Responsive: stack on mobile
- Dark background matching #1e1e2e / #181825
  </action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal/src && npx ng build --configuration production 2>&1 | tail -5</automated>
  </verify>
  <done>Analysis panel renders all sections with correct styling</done>
</task>

<task type="auto">
  <name>Task 3: Add practice score and badges to tile-header</name>
  <files>
    src/src/app/components/tile-header/tile-header.component.ts
    src/src/app/components/tile-header/tile-header.component.html
    src/src/app/components/tile-header/tile-header.component.css
  </files>
  <action>
**TypeScript:**
- Add `@Input() practiceScore: number | null = null` (null = not loaded yet)
- Add `@Input() badges: string[] = []`
- Add getter for score color (green >70, yellow >40, red <=40)
- Import SessionPracticeScore type

**HTML:**
- In header-line-2 (or new header-line-3): show score as small number + badges as chips
- Score: `<span class="practice-score">{{ practiceScore }}</span>` (only if not null)
- Badges: `<span class="badge" *ngFor="let badge of badges">{{ badge }}</span>`
- Compact layout, doesn't take much space

**CSS:**
- `.practice-score`: small font (11px), color based on score range
- `.badge`: tiny colored label chips (font-size 9px, padding 1px 4px, border-radius 3px)
- Badge colors: each badge type gets a Catppuccin accent color
- Don't push header height significantly — keep compact
  </action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal/src && npx ng build --configuration production 2>&1 | tail -5</automated>
  </verify>
  <done>Tile headers show score and badges when data is provided</done>
</task>

<task type="auto">
  <name>Task 4: Wire dashboard to load and pass session scores</name>
  <files>
    src/src/app/components/dashboard/dashboard.component.ts
  </files>
  <action>
- Inject LogAnalysisService
- Add `sessionScores: Map<string, SessionPracticeScore>` property
- On init (or after sessions load): for each active session, call `loadSessionScore(sessionId)`
- Store results in map
- Refresh scores every 60 seconds (or on analysis panel toggle)
- Pass `practiceScore` and `badges` to tile-header via template bindings:
  `[practiceScore]="sessionScores.get(session.metadata.sessionId)?.score ?? null"`
  `[badges]="sessionScores.get(session.metadata.sessionId)?.badges ?? []"`
  </action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal/src && npx ng build --configuration production 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard loads scores and passes them to tile headers</done>
</task>

<task type="auto">
  <name>Task 5: Integrate analysis panel into app component</name>
  <files>
    src/src/app/app.component.ts
    src/src/app/app.component.html
    src/src/app/app.component.css
  </files>
  <action>
**TypeScript:**
- Import AnalysisPanelComponent
- Add to `imports` array in @Component
- Add `showAnalysis = false` property
- Add `toggleAnalysis()` method

**HTML:**
- Add toggle button in header (between mute and LAN URL):
  `<button class="analysis-btn" (click)="showAnalysis = !showAnalysis">Analysis</button>`
- Add panel below header, above dashboard:
  `<app-analysis-panel *ngIf="showAnalysis"></app-analysis-panel>`

**CSS:**
- `.analysis-btn`: style matching existing mute-btn pattern
- Panel area: max-height with overflow-y scroll, border-bottom separator
  </action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal/src && npx ng build --configuration production 2>&1 | tail -5</automated>
  </verify>
  <done>Analysis button in header toggles panel, panel shows above dashboard</done>
</task>

</tasks>

<verification>
1. `cd src && npx ng build --configuration production` — Angular compiles without errors
2. `npx tsc --noEmit` — Root TypeScript compilation passes
3. Visual: Analysis button appears in header, clicking opens panel
4. Visual: Panel shows tool bars, token stats, colored recommendation cards
5. Visual: Tile headers show practice score number and badge chips
6. Remote browser: Panel and scores work via HTTP API
7. Performance: Panel opens in <3 seconds
</verification>

<success_criteria>
- Analysis panel opens/closes via header button
- Tool usage shows as horizontal CSS bars with Catppuccin colors
- Recommendations display with correct severity colors (praise=green, warning=peach etc.)
- Tile headers show per-session practice score and badge chips
- Dashboard refreshes scores periodically
- Works in both Electron and remote browser mode
- No chart libraries — pure CSS visualization
</success_criteria>

<output>
After completion, create `.planning/phases/06-session-log-analysis/06-02-SUMMARY.md`
</output>
