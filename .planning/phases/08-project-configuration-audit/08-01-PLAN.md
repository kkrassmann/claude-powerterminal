---
phase: 08-project-configuration-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/audit-types.ts
  - electron/analysis/audit-engine.ts
  - electron/analysis/audit-prompt.md
autonomous: true
requirements:
  - AUD-02

must_haves:
  truths:
    - "runProjectAudit(projectPath) returns a ProjectAuditResult with overallScore, improvementPotential, and per-file findings"
    - "discoverClaudeProjects() returns a list of valid, existing project paths from ~/.claude/projects/"
    - "audit-prompt.md contains at least 10 named rules (CMD-*, SKL-*, AGT-*, MCP-*) that the engine parses at runtime"
    - "AuditRule, AuditFinding, AuditFileResult, ProjectAuditResult interfaces are exported from src/shared/audit-types.ts"
  artifacts:
    - path: "src/shared/audit-types.ts"
      provides: "Shared TypeScript interfaces for audit results"
      exports: ["AuditRule", "AuditFinding", "AuditFileResult", "ProjectAuditResult"]
    - path: "electron/analysis/audit-engine.ts"
      provides: "Heuristic audit engine + project discovery"
      exports: ["runProjectAudit", "discoverClaudeProjects", "loadAuditRules"]
    - path: "electron/analysis/audit-prompt.md"
      provides: "Machine-parseable rule checklist"
      contains: "### RULE CMD-01"
  key_links:
    - from: "electron/analysis/audit-engine.ts"
      to: "electron/analysis/audit-prompt.md"
      via: "fs.readFileSync at runtime in loadAuditRules()"
      pattern: "loadAuditRules|readFileSync.*audit-prompt"
    - from: "electron/analysis/audit-engine.ts"
      to: "src/shared/audit-types.ts"
      via: "import of AuditRule, AuditFinding, AuditFileResult, ProjectAuditResult"
      pattern: "import.*audit-types"
---

<objective>
Create the heuristic audit engine backend: shared TypeScript interfaces, the audit-prompt.md rule checklist, and the audit-engine.ts module that discovers Claude projects and evaluates configuration files against the rules.

Purpose: This is the core intelligence of Phase 8 — a deterministic, offline rule engine that reads CLAUDE.md, skill definitions, agent configs, and MCP configs from any Claude project and produces scored findings. No LLM API call, no streaming — pure fs.readFileSync + regex.

Output: audit-types.ts (shared interfaces), audit-engine.ts (engine + project discovery), audit-prompt.md (versioned rules)
</objective>

<execution_context>
@C:/Users/Konstantin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Konstantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-project-configuration-audit/08-CONTEXT.md
@.planning/phases/08-project-configuration-audit/08-RESEARCH.md
@electron/analysis/log-analyzer.ts
@src/shared/analysis-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared types + audit engine</name>
  <files>
    src/shared/audit-types.ts
    electron/analysis/audit-engine.ts
  </files>
  <action>
Create `src/shared/audit-types.ts` with these exported interfaces:

```typescript
export type AuditCategory = 'claude-md' | 'skill' | 'agent' | 'mcp';
export type AuditSeverity = 'praise' | 'tip' | 'warning' | 'anti-pattern';
export type AuditCheckType = 'section-exists' | 'length-check' | 'content-regex' | 'file-exists' | 'yaml-key';

export interface AuditRule {
  id: string;           // e.g., "CMD-01"
  category: AuditCategory;
  severity: AuditSeverity;
  check: AuditCheckType;
  pattern?: string;
  min?: number;
  max?: number;
  fix: string;
}

export interface AuditFinding {
  ruleId: string;
  severity: AuditSeverity;
  passed: boolean;
  detail: string;
  fix: string;
}

export interface AuditFileResult {
  filePath: string;
  fileType: AuditCategory;
  displayName: string;
  score: number;            // 0-100
  findings: AuditFinding[]; // Only failed findings
}

export interface ProjectAuditResult {
  projectPath: string;
  projectName: string;
  overallScore: number;          // 0-100 weighted average
  improvementPotential: number;  // 100 - overallScore
  files: AuditFileResult[];
  cachedAt: number;
}
```

Create `electron/analysis/audit-engine.ts` implementing:

**loadAuditRules():** Reads `audit-prompt.md` from `__dirname` using `fs.readFileSync`. Parses with regex:
- Find `### RULE ([\w]+-\d+)` blocks (H3 headers)
- For each block, extract key-value pairs using `\*\*(\w[\w\s-]+)\*\*:\s*(.+)` regex
- Map keys: Category→category, Severity→severity, Check→check, Pattern→pattern, Min→min (parseInt), Max→max (parseInt), Fix→fix
- Skip rules missing fix field (console.warn with rule ID)
- Returns AuditRule[]

**discoverClaudeProjects():** Reads `~/.claude/projects/` via `path.join(os.homedir(), '.claude', 'projects')`. For each directory entry, decode path with `decodeClaudeProjectDir()`:
- Windows: match `/^([A-Z])--(.+)$/` → `${match[1]}:/${match[2].replace(/--/g, '/')}`
- Unix: starts with `-` → `'/' + encoded.slice(1).replace(/--/g, '/')`
- Filter: only return paths where `fs.existsSync(p) && fs.statSync(p).isDirectory()`
- Return sorted string[]

**discoverAuditFiles(projectPath):** Returns array of `{path, fileType, displayName}` for:
1. `CLAUDE.md` at project root (always include, even if not found — engine handles missing)
2. All `.md` files under `.claude/commands/` recursively (fileType: 'skill')
3. All `.md` files under `.claude/agents/` recursively (fileType: 'agent')
4. `.claude/settings.json` if exists (fileType: 'mcp')
5. `.mcp.json` at project root if exists (fileType: 'mcp')

Use a recursive `findMdFiles(dir)` helper that catches errors with try/catch and skips unreadable dirs.

**evaluateRule(rule, content, lines, projectPath):** Returns boolean:
- `section-exists`: `new RegExp(rule.pattern!, 'im').test(content)` — safe for files that don't exist (empty content → false)
- `length-check`: `(rule.min === undefined || lines.length >= rule.min) && (rule.max === undefined || lines.length <= rule.max)` — if file doesn't exist (content=''), lines=[''] (length=1), fails min check
- `content-regex`: same as section-exists
- `file-exists`: `rule.pattern ? fs.existsSync(path.join(projectPath, rule.pattern)) : false`
- `yaml-key`: `rule.pattern ? new RegExp(rule.pattern).test(content) : false`
- default: return true

**runProjectAudit(projectPath):**
1. Load rules via loadAuditRules()
2. Discover files via discoverAuditFiles(projectPath)
3. For each file: read content with `fs.existsSync(f.path) ? fs.readFileSync(f.path, 'utf-8') : ''`
4. Filter applicable rules by fileType === f.fileType
5. Evaluate each rule → AuditFinding[]
6. Score = `Math.round((passedCount / applicable.length) * 100)` — if no rules applicable, score = 100
7. Only include FAILED findings in AuditFileResult.findings
8. Compute overallScore via weighted average: claude-md=0.40, skill=0.30, agent=0.20, mcp=0.10. Group files by fileType, average scores within group, then apply weights (skip missing groups with weight redistribution: divide by sum of present weights)
9. Return ProjectAuditResult with cachedAt: Date.now()

IMPORTANT: Import AuditRule, AuditFinding, AuditFileResult, ProjectAuditResult from `../../src/shared/audit-types` (relative path from electron/analysis/).
Do NOT use readline streaming — config files are small, fs.readFileSync is correct.
Do NOT make any LLM/Claude API calls — pure heuristics only.
</action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal && npx tsc --noEmit --project electron/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify audit-types.ts exports all 4 interfaces. Verify audit-engine.ts imports from shared types and exports runProjectAudit, discoverClaudeProjects, loadAuditRules.</manual>
  </verify>
  <done>TypeScript compilation passes with no errors in audit-engine.ts and audit-types.ts. Functions are exported and typed correctly.</done>
</task>

<task type="auto">
  <name>Task 2: audit-prompt.md rule checklist</name>
  <files>
    electron/analysis/audit-prompt.md
  </files>
  <action>
Create `electron/analysis/audit-prompt.md` as the versioned, machine-parseable rule checklist. The engine parses this file at runtime — formatting must be exact.

**Format requirements (critical for parser):**
- Rule headers must be exactly `### RULE {ID}` (H3, three hashes)
- Key-value pairs must be `**Key:** Value` on their own line (bold key, colon, space, value)
- No blank lines within a key-value block of a rule
- Each rule needs at minimum: Category, Severity, Check, Fix

Write the following rules verbatim:

```markdown
# Claude Project Configuration Audit Rules

Machine-parseable rule checklist for the audit engine.
Each RULE block defines one heuristic check applied against project configuration files.
Format: ### RULE {CATEGORY}-{NN} followed by **Key:** Value pairs.

## CLAUDE.md Rules

### RULE CMD-01
**Category:** claude-md
**Severity:** warning
**Check:** file-exists
**Pattern:** CLAUDE.md
**Fix:** Add a CLAUDE.md file in the project root. This is the primary guidance file Claude reads at the start of every session.

### RULE CMD-02
**Category:** claude-md
**Severity:** anti-pattern
**Check:** length-check
**Min:** 20
**Max:** 800
**Fix:** CLAUDE.md should be 20-800 lines. Too short means insufficient guidance. Too long means Claude may skip or truncate sections.

### RULE CMD-03
**Category:** claude-md
**Severity:** tip
**Check:** section-exists
**Pattern:** ## (Build|Commands|Run|Setup)
**Fix:** Add a "## Build & Run Commands" section listing how to build, test, and run the project. Claude uses this constantly.

### RULE CMD-04
**Category:** claude-md
**Severity:** warning
**Check:** section-exists
**Pattern:** ## (Critical|Rules|MUST|Wichtig|NEVER)
**Fix:** Add a "## Critical Rules" or "## MUST" section for non-negotiable conventions. Without it, Claude guesses.

### RULE CMD-05
**Category:** claude-md
**Severity:** tip
**Check:** content-regex
**Pattern:** (NEVER|ALWAYS|MUST|never|always|must)
**Fix:** Use strong directive language (NEVER, ALWAYS, MUST) to make rules unambiguous for Claude. Soft suggestions are often ignored.

### RULE CMD-06
**Category:** claude-md
**Severity:** tip
**Check:** section-exists
**Pattern:** ## (Tech Stack|Stack|Dependencies|Technologie)
**Fix:** Add a "## Tech Stack" section listing key dependencies and versions. This prevents Claude from suggesting outdated alternatives.

### RULE CMD-07
**Category:** claude-md
**Severity:** warning
**Check:** content-regex
**Pattern:** (test|Test|TEST|spec|Spec)
**Fix:** Document how to run tests in CLAUDE.md. Claude needs to know the test command to verify its own changes.

## Skill Rules

### RULE SKL-01
**Category:** skill
**Severity:** warning
**Check:** section-exists
**Pattern:** ^---
**Fix:** Add YAML frontmatter (---) block with name, description, and allowed-tools fields. Without frontmatter, Claude may not recognize this as a skill.

### RULE SKL-02
**Category:** skill
**Severity:** tip
**Check:** content-regex
**Pattern:** description:
**Fix:** Add a "description:" field in frontmatter so Claude knows when to invoke this skill automatically.

### RULE SKL-03
**Category:** skill
**Severity:** tip
**Check:** content-regex
**Pattern:** (allowed-tools:|tools:)
**Fix:** Add an "allowed-tools:" list to restrict which tools this skill can use. Unrestricted skills are harder to reason about and may use unnecessary tools.

## Agent Rules

### RULE AGT-01
**Category:** agent
**Severity:** warning
**Check:** section-exists
**Pattern:** ^---
**Fix:** Add YAML frontmatter with name, description, and tools fields. Without frontmatter, this agent definition may not be parsed correctly by Claude.

### RULE AGT-02
**Category:** agent
**Severity:** warning
**Check:** content-regex
**Pattern:** tools:
**Fix:** Define a "tools:" list to restrict which tools this agent can use. Unrestricted agents have unbounded capabilities and are harder to control.

### RULE AGT-03
**Category:** agent
**Severity:** warning
**Check:** content-regex
**Pattern:** (<role>|## Role|## Goal|## Responsibilities)
**Fix:** Add a clear <role> block or ## Role/## Goal section defining what this agent does and its boundaries.

### RULE AGT-04
**Category:** agent
**Severity:** tip
**Check:** length-check
**Min:** 10
**Max:** 400
**Fix:** Agent definitions should be 10-400 lines. Too short means vague role. Too long means the agent prompt itself consumes too much context.

## MCP Server Config Rules

### RULE MCP-01
**Category:** mcp
**Severity:** warning
**Check:** content-regex
**Pattern:** (mcpServers|mcp_servers|"mcp")
**Fix:** MCP config should define at least one mcpServers entry. An empty config file provides no value.

### RULE MCP-02
**Category:** mcp
**Severity:** tip
**Check:** content-regex
**Pattern:** "command":\s*"
**Fix:** Each MCP server entry needs a "command" field pointing to the server executable. Without it, Claude cannot launch the server.

### RULE MCP-03
**Category:** mcp
**Severity:** tip
**Check:** content-regex
**Pattern:** "args":\s*\[
**Fix:** Add an "args" array to each MCP server entry specifying the launch arguments. Required for most MCP server implementations.
```

After writing the file, verify the engine can parse it by running a quick Node.js check:
```bash
node -e "
const fs = require('fs');
const content = fs.readFileSync('./electron/analysis/audit-prompt.md', 'utf-8');
const matches = [...content.matchAll(/^### RULE (\w+-\d+)/gm)];
console.log('Rules found:', matches.length, matches.map(m => m[1]).join(', '));
"
```
Expected: 17+ rules found (CMD-01 through CMD-07, SKL-01 through SKL-03, AGT-01 through AGT-04, MCP-01 through MCP-03).
</action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal && node -e "const fs=require('fs');const c=fs.readFileSync('./electron/analysis/audit-prompt.md','utf-8');const m=[...c.matchAll(/^### RULE (\w+-\d+)/gm)];console.log('Rules:',m.length);process.exit(m.length>=17?0:1)"</automated>
    <manual>Open audit-prompt.md and verify rule blocks have correct ### RULE {ID} headers and **Key:** Value formatting.</manual>
  </verify>
  <done>audit-prompt.md exists with 17+ parseable rule blocks. Node.js regex test finds all rule IDs. Engine can load and parse rules without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project electron/tsconfig.json` — no errors in new files
2. `node -e "const e=require('./electron/analysis/audit-engine'); console.log(typeof e.runProjectAudit, typeof e.discoverClaudeProjects)"` — prints "function function"
3. Rule count check: 17+ rules parsed from audit-prompt.md
4. `discoverClaudeProjects()` returns non-empty array containing at least the current project path
</verification>

<success_criteria>
- src/shared/audit-types.ts exports AuditRule, AuditFinding, AuditFileResult, ProjectAuditResult with correct types
- electron/analysis/audit-engine.ts exports runProjectAudit, discoverClaudeProjects, loadAuditRules
- electron/analysis/audit-prompt.md contains 17+ named RULE blocks parseable by the engine
- TypeScript compilation passes with no new errors
- discoverClaudeProjects() correctly decodes ~/.claude/projects/ dir names to real paths
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-configuration-audit/08-01-SUMMARY.md`
</output>
