---
phase: 07-advanced-recommendations-engine
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - src/src/app/services/log-analysis.service.ts
  - src/src/app/components/session-detail/session-detail.component.ts
  - src/src/app/components/session-detail/session-detail.component.html
  - src/src/app/components/session-detail/session-detail.component.css
  - src/src/app/components/analysis-panel/analysis-panel.component.ts
  - src/src/app/components/analysis-panel/analysis-panel.component.html
  - src/src/app/components/analysis-panel/analysis-panel.component.css
  - src/src/app/components/tile-header/tile-header.component.ts
  - src/src/app/components/tile-header/tile-header.component.html
  - src/src/app/components/tile-header/tile-header.component.css
  - src/src/app/app.component.ts
  - src/src/app/app.component.html
  - src/src/app/app.component.css
autonomous: false
requirements: [OPT-05, OPT-06]

must_haves:
  truths:
    - "Clicking the score chip in a tile header opens a session detail panel showing all 5 sub-scores and detected anti-patterns with turn references"
    - "Anti-patterns appear with red styling and show the specific tool call detail text"
    - "Recommendations in the detail panel are sorted: anti-pattern (red) → warning (orange) → tip (blue) → praise (green) → achievement (gold)"
    - "Analysis panel has a Trends section with 6 inline SVG sparklines (one per score dimension + anti-pattern count)"
    - "Tile header badge chips show emoji icons and achievement-gold color (#f6c90e) for Context Master, Zero Error, Planner, Parallel Pro, Speed Demon, Researcher badges"
    - "Trends section shows 'Keine Verlaufsdaten' placeholder when fewer than 2 sessions in history"
  artifacts:
    - path: "src/src/app/components/session-detail/session-detail.component.ts"
      provides: "Per-session drill-down panel with score breakdown and anti-pattern list"
      contains: "SessionDetailComponent"
    - path: "src/src/app/components/analysis-panel/analysis-panel.component.html"
      provides: "Trends section with sparkline SVGs below existing recommendations section"
      contains: "sparkline"
    - path: "src/src/app/components/tile-header/tile-header.component.ts"
      provides: "Clickable score chip emitting sessionSelected event, emoji badge icons"
      contains: "sessionSelected"
    - path: "src/src/app/services/log-analysis.service.ts"
      provides: "loadSessionDetail() and loadTrends() methods using dual-mode IPC/HTTP"
      contains: "loadSessionDetail"
  key_links:
    - from: "src/src/app/components/tile-header/tile-header.component.ts"
      to: "src/src/app/app.component.ts"
      via: "(sessionSelected) event binding, selectedSessionId state"
      pattern: "sessionSelected"
    - from: "src/src/app/app.component.ts"
      to: "src/src/app/components/session-detail/session-detail.component.ts"
      via: "*ngIf=\"selectedSessionId\" [sessionId] input"
      pattern: "selectedSessionId"
    - from: "src/src/app/components/session-detail/session-detail.component.ts"
      to: "src/src/app/services/log-analysis.service.ts"
      via: "loadSessionDetail(sessionId) in ngOnInit"
      pattern: "loadSessionDetail"
    - from: "src/src/app/components/analysis-panel/analysis-panel.component.ts"
      to: "src/src/app/services/log-analysis.service.ts"
      via: "loadTrends() called on panel open"
      pattern: "loadTrends"
---

<objective>
Build the Phase 7 Angular UI: a session detail panel (click to drill into per-session scores and anti-patterns), sparkline trend charts in the analysis panel, and upgraded tile-header badges with emoji icons and gold color. Depends on Plan 01 (extended types + backend) and Plan 02 (new IPC channels + score history).

Purpose: All backend work from Plans 01 and 02 becomes visible to the user through these UI components.
Output: SessionDetailComponent (new), extended AnalysisPanelComponent (Trends section), upgraded TileHeaderComponent (clickable score, emoji badges).
</objective>

<execution_context>
@C:/Users/Konstantin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Konstantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-recommendations-engine/07-CONTEXT.md
@.planning/phases/07-advanced-recommendations-engine/07-RESEARCH.md
@.planning/phases/06-session-log-analysis/06-02-SUMMARY.md
@.planning/phases/07-advanced-recommendations-engine/07-01-SUMMARY.md
@.planning/phases/07-advanced-recommendations-engine/07-02-SUMMARY.md
@src/src/app/services/log-analysis.service.ts
@src/src/app/components/tile-header/tile-header.component.ts
@src/src/app/components/analysis-panel/analysis-panel.component.ts
@src/src/app/app.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Angular service and create SessionDetailComponent</name>
  <files>
    src/src/app/services/log-analysis.service.ts,
    src/src/app/components/session-detail/session-detail.component.ts,
    src/src/app/components/session-detail/session-detail.component.html,
    src/src/app/components/session-detail/session-detail.component.css
  </files>
  <action>
    **Part A: Extend log-analysis.service.ts**

    Add two new methods following the existing dual-mode IPC/HTTP pattern (check `window.electronAPI` exists → use IPC, otherwise → use HTTP fetch):

    ```typescript
    async loadSessionDetail(sessionId: string): Promise<SessionScoreDetail | null> {
      try {
        if (window.electronAPI?.ipcRenderer) {
          return await window.electronAPI.ipcRenderer.invoke('analysis:session-detail', sessionId);
        } else {
          const res = await fetch(`/api/analysis/session-detail?sessionId=${encodeURIComponent(sessionId)}`);
          return await res.json();
        }
      } catch {
        return null;
      }
    }

    async loadTrends(): Promise<ScoreTrends | null> {
      try {
        if (window.electronAPI?.ipcRenderer) {
          return await window.electronAPI.ipcRenderer.invoke('analysis:score-trends');
        } else {
          const res = await fetch('/api/analysis/trends');
          return await res.json();
        }
      } catch {
        return null;
      }
    }
    ```

    Import `SessionScoreDetail` and `ScoreTrends` from `../../../../shared/analysis-types`.

    **Part B: Create SessionDetailComponent**

    Create `src/src/app/components/session-detail/` directory and three files:

    **session-detail.component.ts:**
    ```typescript
    import { Component, Input, Output, EventEmitter, OnInit } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import { LogAnalysisService } from '../../services/log-analysis.service';
    import { SessionScoreDetail, AntiPatternOccurrence, Recommendation } from '../../../../../shared/analysis-types';

    @Component({
      selector: 'app-session-detail',
      standalone: true,
      imports: [CommonModule],
      templateUrl: './session-detail.component.html',
      styleUrls: ['./session-detail.component.css'],
    })
    export class SessionDetailComponent implements OnInit {
      @Input() sessionId!: string;
      @Output() close = new EventEmitter<void>();

      detail: SessionScoreDetail | null = null;
      loading = true;
      error = false;

      // Sorted recommendations: anti-pattern → warning → tip → praise → achievement
      readonly severityOrder = ['anti-pattern', 'warning', 'tip', 'praise', 'achievement'];

      constructor(private analysisService: LogAnalysisService) {}

      async ngOnInit(): Promise<void> {
        try {
          this.detail = await this.analysisService.loadSessionDetail(this.sessionId);
          if (this.detail?.recommendations) {
            // Sort recommendations by severity order
            this.detail = {
              ...this.detail,
              recommendations: [...this.detail.recommendations].sort(
                (a, b) => this.severityOrder.indexOf(a.severity) - this.severityOrder.indexOf(b.severity)
              ),
            };
          }
        } catch {
          this.error = true;
        } finally {
          this.loading = false;
        }
      }

      getScoreColor(score: number): string {
        if (score > 70) return '#a6e3a1';
        if (score > 40) return '#f9e2af';
        return '#f38ba8';
      }

      getAntiPatternLabel(pattern: string): string {
        const labels: Record<string, string> = {
          'bash-for-file-ops': 'Bash statt Grep/Read',
          'correction-loop': 'Korrektur-Schleife',
          'kitchen-sink': 'Zu breite Session',
          'infinite-exploration': 'Infinite Exploration',
        };
        return labels[pattern] ?? pattern;
      }

      onClose(): void {
        this.close.emit();
      }
    }
    ```

    **session-detail.component.html:**
    ```html
    <div class="detail-overlay" (click)="onClose()">
      <div class="detail-panel" (click)="$event.stopPropagation()">
        <div class="detail-header">
          <span class="detail-title">Session Analyse</span>
          <span class="session-id-label">{{ sessionId | slice:0:8 }}...</span>
          <button class="close-btn" (click)="onClose()">✕</button>
        </div>

        <div *ngIf="loading" class="loading-state">Lade Analyse...</div>
        <div *ngIf="error" class="error-state">Fehler beim Laden der Session-Details.</div>

        <div *ngIf="detail && !loading" class="detail-content">
          <!-- Overall score -->
          <div class="overall-score" [style.color]="getScoreColor(detail.score)">
            {{ detail.score }}/100
          </div>

          <!-- 5-dimension breakdown -->
          <section class="score-breakdown">
            <h3>Score-Aufschlüsselung</h3>
            <div class="dimension-row">
              <span class="dim-label">Tool-Nativeness</span>
              <div class="dim-bar-wrap">
                <div class="dim-bar" [style.width.%]="detail.toolNativenessScore" [style.background]="getScoreColor(detail.toolNativenessScore)"></div>
              </div>
              <span class="dim-value">{{ detail.toolNativenessScore }}</span>
            </div>
            <div class="dimension-row">
              <span class="dim-label">Subagent-Nutzung</span>
              <div class="dim-bar-wrap">
                <div class="dim-bar" [style.width.%]="detail.subagentScore" [style.background]="getScoreColor(detail.subagentScore)"></div>
              </div>
              <span class="dim-value">{{ detail.subagentScore }}</span>
            </div>
            <div class="dimension-row">
              <span class="dim-label">Read-before-Write</span>
              <div class="dim-bar-wrap">
                <div class="dim-bar" [style.width.%]="detail.readBeforeWriteScore" [style.background]="getScoreColor(detail.readBeforeWriteScore)"></div>
              </div>
              <span class="dim-value">{{ detail.readBeforeWriteScore }}</span>
            </div>
            <div class="dimension-row">
              <span class="dim-label">Context-Effizienz</span>
              <div class="dim-bar-wrap">
                <div class="dim-bar" [style.width.%]="detail.contextEfficiencyScore" [style.background]="getScoreColor(detail.contextEfficiencyScore)"></div>
              </div>
              <span class="dim-value">{{ detail.contextEfficiencyScore }}</span>
            </div>
            <div class="dimension-row">
              <span class="dim-label">Fehlerrate</span>
              <div class="dim-bar-wrap">
                <div class="dim-bar" [style.width.%]="detail.errorScore" [style.background]="getScoreColor(detail.errorScore)"></div>
              </div>
              <span class="dim-value">{{ detail.errorScore }}</span>
            </div>
          </section>

          <!-- Anti-patterns -->
          <section class="anti-patterns" *ngIf="detail.antiPatterns && detail.antiPatterns.length > 0">
            <h3>Anti-Patterns ({{ detail.antiPatterns.length }})</h3>
            <div class="anti-pattern-card" *ngFor="let ap of detail.antiPatterns">
              <span class="ap-label">{{ getAntiPatternLabel(ap.pattern) }}</span>
              <span class="ap-turn" *ngIf="ap.turn > 0">Turn {{ ap.turn }}</span>
              <span class="ap-detail">{{ ap.detail }}</span>
            </div>
          </section>
          <section class="anti-patterns" *ngIf="!detail.antiPatterns || detail.antiPatterns.length === 0">
            <h3>Anti-Patterns</h3>
            <span class="no-issues">Keine Anti-Patterns erkannt ✓</span>
          </section>

          <!-- Sorted recommendations (max 10, show all for now) -->
          <section class="recommendations" *ngIf="detail.recommendations && detail.recommendations.length > 0">
            <h3>Empfehlungen</h3>
            <div class="rec-card" *ngFor="let rec of detail.recommendations" [class]="'severity-' + rec.severity">
              <span class="rec-title">{{ rec.title }}</span>
              <span class="rec-desc">{{ rec.description }}</span>
            </div>
          </section>

          <!-- Session metadata -->
          <section class="session-meta" *ngIf="detail.modelUsed || detail.avgTurnDurationMs || detail.compactBoundaryCount">
            <h3>Session-Info</h3>
            <div class="meta-row" *ngIf="detail.modelUsed">
              <span class="meta-label">Modell:</span>
              <span class="meta-value">{{ detail.modelUsed }}</span>
            </div>
            <div class="meta-row" *ngIf="detail.avgTurnDurationMs">
              <span class="meta-label">Ø Turn-Dauer:</span>
              <span class="meta-value">{{ (detail.avgTurnDurationMs / 1000).toFixed(1) }}s</span>
            </div>
            <div class="meta-row" *ngIf="detail.compactBoundaryCount">
              <span class="meta-label">Compact-Events:</span>
              <span class="meta-value">{{ detail.compactBoundaryCount }}</span>
            </div>
          </section>
        </div>
      </div>
    </div>
    ```

    **session-detail.component.css:**
    Catppuccin Mocha dark theme. Key styles:
    ```css
    .detail-overlay {
      position: fixed; inset: 0; background: rgba(17,17,27,0.75);
      display: flex; align-items: center; justify-content: flex-end;
      z-index: 1000;
    }
    .detail-panel {
      width: 420px; height: 100vh; background: #1e1e2e;
      border-left: 1px solid #313244; overflow-y: auto;
      padding: 1.5rem; box-sizing: border-box;
    }
    .detail-header { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 1.5rem; }
    .detail-title { font-size: 1rem; font-weight: 600; color: #cdd6f4; flex: 1; }
    .session-id-label { font-size: 0.7rem; color: #6c7086; font-family: monospace; }
    .close-btn { background: none; border: none; color: #6c7086; cursor: pointer; font-size: 1rem; padding: 0; }
    .close-btn:hover { color: #f38ba8; }
    .overall-score { font-size: 2.5rem; font-weight: 700; text-align: center; margin-bottom: 1.5rem; }
    h3 { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.08em; color: #6c7086; margin: 1rem 0 0.5rem; }
    .dimension-row { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.3rem; }
    .dim-label { font-size: 0.75rem; color: #a6adc8; width: 120px; flex-shrink: 0; }
    .dim-bar-wrap { flex: 1; height: 6px; background: #313244; border-radius: 3px; overflow: hidden; }
    .dim-bar { height: 100%; border-radius: 3px; transition: width 0.3s; }
    .dim-value { font-size: 0.75rem; color: #a6adc8; width: 24px; text-align: right; }
    .anti-pattern-card {
      background: #2a1a1f; border-left: 3px solid #f38ba8;
      border-radius: 4px; padding: 0.5rem 0.75rem; margin-bottom: 0.4rem;
      display: flex; flex-direction: column; gap: 0.15rem;
    }
    .ap-label { font-size: 0.8rem; font-weight: 600; color: #f38ba8; }
    .ap-turn { font-size: 0.7rem; color: #6c7086; }
    .ap-detail { font-size: 0.75rem; color: #a6adc8; font-family: monospace; word-break: break-all; }
    .no-issues { font-size: 0.8rem; color: #a6e3a1; }
    .rec-card {
      border-left: 3px solid #89b4fa; border-radius: 4px; padding: 0.4rem 0.75rem;
      margin-bottom: 0.35rem; background: #181825;
    }
    .rec-card.severity-praise    { border-left-color: #a6e3a1; }
    .rec-card.severity-tip       { border-left-color: #89b4fa; }
    .rec-card.severity-warning   { border-left-color: #fab387; }
    .rec-card.severity-anti-pattern { border-left-color: #f38ba8; background: #2a1a1f; }
    .rec-card.severity-achievement { border-left-color: #f6c90e; background: #1e1c11; }
    .rec-title { display: block; font-size: 0.8rem; font-weight: 600; color: #cdd6f4; }
    .rec-desc { display: block; font-size: 0.75rem; color: #a6adc8; margin-top: 0.1rem; }
    .meta-row { display: flex; gap: 0.5rem; margin-bottom: 0.2rem; }
    .meta-label { font-size: 0.75rem; color: #6c7086; }
    .meta-value { font-size: 0.75rem; color: #a6adc8; font-family: monospace; }
    .loading-state, .error-state { color: #6c7086; font-size: 0.85rem; text-align: center; padding: 2rem; }
    ```
  </action>
  <verify>
    <automated>cd C:/Dev/claude-powerterminal && npx tsc --noEmit --project src/tsconfig.app.json 2>&1 | head -20</automated>
    <manual>Confirm session-detail/ directory has all 3 files. Confirm service has loadSessionDetail() and loadTrends() methods.</manual>
  </verify>
  <done>TypeScript compilation passes. SessionDetailComponent created with selector 'app-session-detail'. LogAnalysisService has loadSessionDetail(sessionId) and loadTrends() methods. Session detail HTML shows score breakdown, anti-patterns list, sorted recommendations.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade tile-header badges and add Trends sparklines to analysis panel</name>
  <files>
    src/src/app/components/tile-header/tile-header.component.ts,
    src/src/app/components/tile-header/tile-header.component.html,
    src/src/app/components/tile-header/tile-header.component.css,
    src/src/app/components/analysis-panel/analysis-panel.component.ts,
    src/src/app/components/analysis-panel/analysis-panel.component.html,
    src/src/app/components/analysis-panel/analysis-panel.component.css
  </files>
  <action>
    **Part A: Upgrade TileHeaderComponent**

    In `tile-header.component.ts`:
    1. Add `@Output() sessionSelected = new EventEmitter<string>()` (exports the sessionId when score chip is clicked)
    2. Add `@Input() sessionId: string = ''` (needed to emit which session was selected)
    3. Add badge emoji map method:
    ```typescript
    getBadgeEmoji(badge: string): string {
      const map: Record<string, string> = {
        'Context Master': '🧠',
        'Zero Error': '✅',
        'Planner': '📋',
        'Parallel Pro': '⚡',
        'Speed Demon': '🚀',
        'Researcher': '🔍',
        'Tool Native': '🛠️',
        'Subagent Pro': '🤝',
        'Context Efficient': '🧠',  // Phase 6 badge alias
        'Orchestrated': '🤝',       // Phase 6 badge alias
        'Planned': '📋',            // Phase 6 badge alias
      };
      return map[badge] ?? '🏅';
    }
    ```
    4. Add `isAchievementBadge(badge: string): boolean` — returns true for the 6 named badges (Context Master, Zero Error, Planner, Parallel Pro, Speed Demon, Researcher)
    5. Add `onScoreClick()` method:
    ```typescript
    onScoreClick(): void {
      if (this.sessionId) {
        this.sessionSelected.emit(this.sessionId);
      }
    }
    ```

    In `tile-header.component.html`:
    - Make the score chip clickable: add `(click)="onScoreClick()"` and `style="cursor:pointer"` to the score element
    - Update badge chips to include emoji: `{{ getBadgeEmoji(badge) }} {{ badge }}`
    - Add `[class.achievement-badge]="isAchievementBadge(badge)"` to badge chip elements

    In `tile-header.component.css`:
    - Add `.achievement-badge { color: #f6c90e; border-color: #f6c90e; }` (achievement gold, distinct from Catppuccin gold)
    - Add `.score-chip { cursor: pointer; }` with hover state: `.score-chip:hover { opacity: 0.8; }`

    **Part B: Add Trends section to AnalysisPanelComponent**

    In `analysis-panel.component.ts`:
    1. Import `ScoreTrends` from `../../../../../shared/analysis-types`
    2. Add `trends: ScoreTrends | null = null` and `trendsExpanded = true` properties
    3. Add `buildSparklinePath(values: number[], width = 60, height = 20): string` method:
    ```typescript
    buildSparklinePath(values: number[], width = 60, height = 20): string {
      if (!values || values.length < 2) return '';
      const min = Math.min(...values);
      const max = Math.max(...values);
      const range = max - min || 1;
      const xStep = width / (values.length - 1);
      const points = values.map((v, i) => {
        const x = i * xStep;
        const y = height - ((v - min) / range) * (height - 2) - 1;
        return `${x.toFixed(1)},${y.toFixed(1)}`;
      });
      return `M${points.join(' L')}`;
    }
    ```
    4. In `ngOnInit()` or wherever analysis is loaded, also call `this.analysisService.loadTrends().then(t => this.trends = t)` — call this AFTER the main analysis loads (or on panel open, whichever pattern is currently used in this component)
    5. Pre-compute sparkline paths in a method `getSparklineData()` that returns an object with paths for each dimension — called once when trends data arrives, cached as a component field. Do NOT compute paths in the template (prevents per-change-detection recalculation).

    In `analysis-panel.component.html`:
    Add a new collapsible "Trends" section below the existing Empfehlungen section:
    ```html
    <!-- Trends section -->
    <div class="section">
      <div class="section-header" (click)="trendsExpanded = !trendsExpanded">
        <span>Verlauf (letzte 10 Sessions)</span>
        <span class="toggle">{{ trendsExpanded ? '▾' : '▸' }}</span>
      </div>
      <div class="section-body" *ngIf="trendsExpanded">
        <div *ngIf="!trends || trends.entries.length < 2" class="no-trends">
          Keine Verlaufsdaten (mindestens 2 Sessions erforderlich)
        </div>
        <div *ngIf="trends && trends.entries.length >= 2" class="sparklines-grid">
          <div class="sparkline-row" *ngFor="let dim of sparklineDimensions">
            <span class="sparkline-label">{{ dim.label }}</span>
            <svg class="sparkline" width="60" height="20" viewBox="0 0 60 20">
              <path [attr.d]="dim.path" [attr.stroke]="dim.color" stroke-width="1.5" fill="none"/>
            </svg>
            <span class="sparkline-last">{{ dim.lastValue }}</span>
          </div>
        </div>
      </div>
    </div>
    ```

    Define `sparklineDimensions` as a computed property (getter or field updated when trends changes):
    ```typescript
    get sparklineDimensions() {
      if (!this.trends || this.trends.entries.length < 2) return [];
      return [
        { label: 'Gesamt', path: this.buildSparklinePath(this.trends.totalScore), color: '#cdd6f4', lastValue: this.trends.totalScore.at(-1) ?? 0 },
        { label: 'Tool-Nativ', path: this.buildSparklinePath(this.trends.toolNativeness), color: '#89b4fa', lastValue: this.trends.toolNativeness.at(-1) ?? 0 },
        { label: 'Subagent', path: this.buildSparklinePath(this.trends.subagent), color: '#a6e3a1', lastValue: this.trends.subagent.at(-1) ?? 0 },
        { label: 'Read/Write', path: this.buildSparklinePath(this.trends.readBeforeWrite), color: '#fab387', lastValue: this.trends.readBeforeWrite.at(-1) ?? 0 },
        { label: 'Context', path: this.buildSparklinePath(this.trends.contextEfficiency), color: '#cba6f7', lastValue: this.trends.contextEfficiency.at(-1) ?? 0 },
        { label: 'Anti-Pattern', path: this.buildSparklinePath(this.trends.antiPatternCount), color: '#f38ba8', lastValue: this.trends.antiPatternCount.at(-1) ?? 0 },
      ];
    }
    ```

    In `analysis-panel.component.css`:
    Add sparkline styles:
    ```css
    .sparklines-grid { display: flex; flex-direction: column; gap: 0.4rem; }
    .sparkline-row { display: flex; align-items: center; gap: 0.75rem; }
    .sparkline-label { font-size: 0.72rem; color: #a6adc8; width: 75px; flex-shrink: 0; }
    .sparkline { flex-shrink: 0; }
    .sparkline-last { font-size: 0.72rem; color: #cdd6f4; width: 24px; text-align: right; }
    .no-trends { font-size: 0.75rem; color: #6c7086; padding: 0.5rem 0; }
    ```
  </action>
  <verify>
    <automated>cd C:/Dev/claude-powerterminal && npx tsc --noEmit --project src/tsconfig.app.json 2>&1 | head -20</automated>
    <manual>Confirm tile-header.component.ts has sessionSelected Output. Confirm analysis-panel.component.ts has buildSparklinePath() and sparklineDimensions getter. Confirm sparkline SVG elements present in analysis-panel.component.html.</manual>
  </verify>
  <done>TypeScript compilation passes. TileHeaderComponent emits sessionSelected on score click. Badge chips show emoji icons. Achievement badges use gold class. AnalysisPanelComponent has Trends section with 6 sparkline rows and 'Keine Verlaufsdaten' guard for fewer than 2 entries.</done>
</task>

<task type="auto">
  <name>Task 3: Wire session-detail panel into app component and dashboard</name>
  <files>
    src/src/app/app.component.ts,
    src/src/app/app.component.html,
    src/src/app/app.component.css,
    src/src/app/components/dashboard/dashboard.component.ts,
    src/src/app/components/dashboard/dashboard.component.html
  </files>
  <action>
    **Part A: app.component.ts**
    1. Import `SessionDetailComponent` and add to `imports` array of the standalone AppComponent
    2. Add `selectedSessionId: string | null = null` property
    3. Add handler: `onSessionSelected(sessionId: string): void { this.selectedSessionId = sessionId === this.selectedSessionId ? null : sessionId; }`
    4. Add handler: `closeSessionDetail(): void { this.selectedSessionId = null; }`

    **Part B: app.component.html**
    1. Add `<app-session-detail>` below the existing `<app-analysis-panel>` (or alongside it):
    ```html
    <app-session-detail
      *ngIf="selectedSessionId"
      [sessionId]="selectedSessionId"
      (close)="closeSessionDetail()">
    </app-session-detail>
    ```
    2. Pass `(sessionSelected)` event from dashboard to app. The DashboardComponent emits up through the component tree. The tile-header already emits `sessionSelected` — dashboard needs to bubble it up.

    **Part C: dashboard.component.ts**
    1. Add `@Output() sessionSelected = new EventEmitter<string>()` to DashboardComponent
    2. Add handler: `onSessionSelected(sessionId: string): void { this.sessionSelected.emit(sessionId); }`

    **Part D: dashboard.component.html**
    1. Find where `<app-tile-header>` is rendered. Add:
       - `[sessionId]="session.id"` (pass session ID to tile-header, needed for its emit)
       - `(sessionSelected)="onSessionSelected($event)"` (catch tile-header's emit and bubble up)
    Note: The `session.id` field name — check the actual Session type in the existing codebase. It may be `session.sessionId` or `session.id`. Use the correct field name from the existing code.

    **Part E: app.component.html**
    Bind to dashboard's `sessionSelected` output:
    ```html
    <app-dashboard
      ...existing bindings...
      (sessionSelected)="onSessionSelected($event)">
    </app-dashboard>
    ```

    **Verification of Angular change detection:**
    The dashboard currently uses `this.sessionScores = new Map(this.sessionScores)` after updating scores (Phase 6 pattern). If adding `antiPatternCount` or other fields to the score result, maintain this pattern so tile-header re-renders.

    **Part F: app.component.css**
    No new styles needed — session-detail panel is positioned fixed via its own CSS.
  </action>
  <verify>
    <automated>cd C:/Dev/claude-powerterminal && npx ng build --configuration production 2>&1 | tail -20</automated>
    <manual>Verify: AppComponent imports SessionDetailComponent. DashboardComponent has sessionSelected Output. TileHeader receives [sessionId] input in dashboard template.</manual>
  </verify>
  <done>Angular production build succeeds with zero errors. SessionDetailComponent is registered in AppComponent imports. Session-selected event chain: tile-header → dashboard → app → session-detail panel renders. *ngIf correctly shows/hides panel based on selectedSessionId.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of Phase 7 UI features</name>
  <what-built>
    All Phase 7 UI features: (1) Clicking a score chip in any tile header opens a session detail panel with score breakdown, anti-patterns with turn references, and sorted recommendations. (2) Analysis panel now has a Trends section with 6 inline SVG sparklines. (3) Badge chips show emoji icons with achievement-gold color for special badges.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd C:/Dev/claude-powerterminal && npm start`
    2. Open the dashboard with at least one active terminal session that has been analyzed
    3. Verify badge chips show emoji icons (e.g., "🛠️ Tool Native", "🤝 Subagent Pro")
    4. Click the score chip (e.g., "73") in any tile header — verify a panel slides in from the right
    5. Verify the session detail panel shows: overall score, 5 dimension bars, anti-patterns section, recommendations sorted by severity
    6. Click the score chip again or click the ✕ button — verify panel closes
    7. Open the analysis panel (Analyse button in header)
    8. Scroll to the bottom — verify a "Verlauf" section is present
    9. If fewer than 2 sessions have been analyzed: verify "Keine Verlaufsdaten" message appears
    10. If 2+ sessions analyzed: verify 6 sparkline rows appear with SVG lines
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 steps pass, or describe which step failed and what was observed</resume-signal>
</task>

</tasks>

<verification>
After all auto tasks complete (before checkpoint):
1. `npx ng build --configuration production` — succeeds with zero errors
2. `grep -rn "sessionSelected" src/src/app/` — found in tile-header, dashboard, app component (3 locations minimum)
3. `grep -n "session-detail" src/src/app/app.component.html` — returns match
4. `grep -n "buildSparklinePath" src/src/app/components/analysis-panel/analysis-panel.component.ts` — returns match
5. `grep -n "achievement-badge\|#f6c90e" src/src/app/components/tile-header/tile-header.component.css` — returns match
</verification>

<success_criteria>
- Angular production build succeeds with zero errors
- SessionDetailComponent exists in session-detail/ directory with 3 files
- TileHeaderComponent emits sessionSelected with sessionId, has getBadgeEmoji() and isAchievementBadge()
- AnalysisPanelComponent has Trends section with sparklines and 'Keine Verlaufsdaten' guard
- LogAnalysisService has loadSessionDetail() and loadTrends() methods
- Full event chain works: tile score click → session detail panel opens → close button/overlay click closes it
- Achievement badges display in gold (#f6c90e)
- Human verification: all 10 steps confirmed by user
</success_criteria>

<output>
After completion, create `.planning/phases/07-advanced-recommendations-engine/07-03-SUMMARY.md`
</output>
