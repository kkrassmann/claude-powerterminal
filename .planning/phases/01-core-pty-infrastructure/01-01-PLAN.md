---
phase: 01-core-pty-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - electron/main.ts
  - electron/preload.ts
  - src/app/models/session.model.ts
  - src/app/models/pty-config.model.ts
  - src/app/services/session-manager.service.ts
  - src/shared/ipc-channels.ts
autonomous: true
requirements:
  - SESS-01

must_haves:
  truths:
    - "Session metadata (session ID, working directory, CLI flags, timestamp) can be saved to disk"
    - "Saved sessions can be loaded from disk on app startup"
    - "Session data is persisted immediately when created or deleted (no batching)"
  artifacts:
    - path: "package.json"
      provides: "Dependencies for Electron, Angular, node-pty, TypeScript"
      contains: "node-pty"
    - path: "electron/main.ts"
      provides: "Electron main process entry point"
      min_lines: 30
    - path: "src/app/models/session.model.ts"
      provides: "SessionMetadata interface definition"
      exports: ["SessionMetadata"]
    - path: "src/app/services/session-manager.service.ts"
      provides: "Session persistence (save/load/delete from JSON)"
      exports: ["SessionManagerService"]
      min_lines: 50
  key_links:
    - from: "src/app/services/session-manager.service.ts"
      to: "sessions.json file"
      via: "fs.writeFileSync/readFileSync"
      pattern: "writeFileSync.*sessions\\.json"
    - from: "src/app/services/session-manager.service.ts"
      to: "src/app/models/session.model.ts"
      via: "TypeScript import"
      pattern: "import.*SessionMetadata"
---

<objective>
Establish the foundation for Claude PowerTerminal by scaffolding the Electron + Angular project structure and implementing session persistence. This creates the data models and storage layer needed for managing Claude CLI sessions across app restarts.

Purpose: Provides the infrastructure for session lifecycle management and crash recovery
Output: Working Electron + Angular skeleton with JSON-based session persistence service
</objective>

<execution_context>
@C:/Users/Konstantin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Konstantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-pty-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-pty-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Electron + Angular project</name>
  <files>
    package.json
    tsconfig.json
    electron/main.ts
    electron/preload.ts
    src/shared/ipc-channels.ts
  </files>
  <action>
Initialize Node.js project and install dependencies:
- `npm init -y` for package.json
- Install Electron, Angular CLI, node-pty, TypeScript, and build tools
- Dependencies: `electron@^28.0.0`, `@angular/cli@^17.0.0`, `node-pty@^1.1.0`, `typescript@^5.0.0`, `node-addon-api@^7.1.0`
- Dev dependencies: `@types/node`, `electron-builder` (for packaging later)

Create Electron main process entry point (electron/main.ts):
- Import necessary Electron modules (app, BrowserWindow, ipcMain)
- Create main window with Angular dev server URL (http://localhost:4200) in development
- Basic window configuration (800x600, webPreferences with preload script)
- App lifecycle (ready, window-all-closed, activate)

Create preload script (electron/preload.ts):
- Context bridge setup for secure IPC communication
- Expose `window.electronAPI` with typed invoke/on methods
- Type-safe wrapper around ipcRenderer

Create IPC channel constants (src/shared/ipc-channels.ts):
- Define all IPC channel names as constants (PTY_SPAWN, PTY_KILL, PTY_DATA, PTY_EXIT, SESSION_SAVE, SESSION_LOAD, SESSION_DELETE)
- Export as const object for type safety

Configure TypeScript (tsconfig.json):
- Target ES2020, module ESNext
- Separate configurations for Electron (Node.js) and Angular (browser) if needed
- Enable strict mode, esModuleInterop

Add npm scripts to package.json:
- `start:electron`: Launch Electron in development
- `start:angular`: Launch Angular dev server
- `dev`: Run both concurrently (use concurrently package)
  </action>
  <verify>
Run `npm install` successfully (no errors)
Run `npm run dev` and verify Electron window opens with Angular placeholder content
Check node_modules/ contains node-pty, electron, @angular packages
  </verify>
  <done>
Electron window launches successfully
Angular dev server runs and loads in Electron
All dependencies installed without compilation errors
IPC infrastructure types defined and ready for use
  </done>
</task>

<task type="auto">
  <name>Task 2: Create session data models</name>
  <files>
    src/app/models/session.model.ts
    src/app/models/pty-config.model.ts
  </files>
  <action>
Create SessionMetadata interface (src/app/models/session.model.ts):
- Fields: sessionId (string), workingDirectory (string), cliFlags (string[]), createdAt (string - ISO 8601 timestamp)
- Export interface for use in services and components
- Add JSDoc comments explaining each field

Create PTYSpawnOptions interface (src/app/models/pty-config.model.ts):
- Fields: sessionId (string), cwd (string), flags (string[])
- Used for IPC communication between renderer and main process
- Matches the structure needed by node-pty spawn options
- Export interface

Follow TypeScript best practices:
- Use readonly for immutable fields if applicable
- Use string literal types for known flag values if helpful
- Keep models pure (no logic, just type definitions)
  </action>
  <verify>
TypeScript compilation succeeds (`npm run build` or tsc check)
Import statements work in other files
No TypeScript errors in models directory
  </verify>
  <done>
SessionMetadata interface defines all required fields (sessionId, workingDirectory, cliFlags, createdAt)
PTYSpawnOptions interface ready for IPC usage
Models compile without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement session persistence service</name>
  <files>
    src/app/services/session-manager.service.ts
  </files>
  <action>
Create SessionManagerService as Angular injectable service (providedIn: 'root'):

Methods to implement:
1. `saveSession(session: SessionMetadata): void`
   - Load existing sessions from JSON file
   - Append new session to array
   - Write immediately using fs.writeFileSync (synchronous for durability)
   - File location: path.join(app.getPath('userData'), 'sessions.json')
   - Use JSON.stringify with 2-space indentation for readability

2. `deleteSession(sessionId: string): void`
   - Load existing sessions
   - Filter out session with matching sessionId
   - Write updated array back to file (synchronous)

3. `loadSessions(): SessionMetadata[]`
   - Read sessions.json using fs.readFileSync
   - Parse JSON and return array
   - Return empty array if file doesn't exist or parse fails (don't crash)
   - Wrap in try/catch for error handling

4. `getSession(sessionId: string): SessionMetadata | undefined`
   - Load all sessions
   - Find and return session by ID
   - Return undefined if not found

Implementation notes:
- Use synchronous file I/O (fs.writeFileSync, fs.readFileSync) per user decision
- Import fs from 'fs' and path from 'path'
- Import app from 'electron' via Angular injection or window.electronAPI
- File path: store in userData directory (cross-platform location)
- Error handling: log errors but don't throw (graceful degradation)
- IMPORTANT: This service runs in the renderer process but needs Node.js APIs. We'll need to expose file operations via IPC in main process. For now, structure the service to use IPC calls instead of direct fs access.

Actually, correction based on Electron architecture:
- SessionManagerService should use IPC to communicate with main process
- Main process handles actual file I/O
- Service methods become async and use window.electronAPI.invoke()
- Create IPC handlers in main process for SESSION_SAVE, SESSION_LOAD, SESSION_DELETE channels

Revised implementation:
- Methods return Promise<void> or Promise<SessionMetadata[]>
- Use window.electronAPI.invoke(IPC_CHANNELS.SESSION_SAVE, session)
- Use window.electronAPI.invoke(IPC_CHANNELS.SESSION_LOAD)
- Use window.electronAPI.invoke(IPC_CHANNELS.SESSION_DELETE, sessionId)
- Add proper TypeScript typing for window.electronAPI (declare in service or types file)
  </action>
  <verify>
TypeScript compilation succeeds
Service is injectable (check @Injectable decorator)
Methods have correct signatures (async, return types)
No direct fs/path imports in renderer-side service (should use IPC)
  </verify>
  <done>
SessionManagerService exists with saveSession, deleteSession, loadSessions, getSession methods
Service uses IPC for communication with main process (no direct file access in renderer)
TypeScript types are correct (async methods, proper return types)
Service is ready to be consumed by components and other services
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `npm install` - should complete without errors
2. Run `npm run dev` - Electron window should open with Angular app running
3. Check file structure - all model and service files exist
4. TypeScript compilation - no errors in models or services
5. IPC channels defined - src/shared/ipc-channels.ts exports all required channel names
</verification>

<success_criteria>
- Electron + Angular project structure exists and builds successfully
- SessionMetadata and PTYSpawnOptions models defined with TypeScript types
- SessionManagerService implements save/load/delete operations via IPC
- IPC channel constants defined for future PTY and session operations
- npm scripts allow running development environment (Electron + Angular dev server)
- No TypeScript compilation errors
- Foundation ready for PTY implementation in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pty-infrastructure/01-01-SUMMARY.md`
</output>
