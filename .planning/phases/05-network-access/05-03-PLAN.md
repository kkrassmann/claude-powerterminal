---
phase: 05-network-access
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/http/static-server.ts
  - src/src/app/app.component.ts
autonomous: true
gap_closure: true
requirements: [NET-01, NET-02]

must_haves:
  truths:
    - "Phone browser shows the same session tiles as Electron window"
    - "New sessions created in Electron appear on phone without manual reload"
    - "Session count matches between Electron and remote browsers"
  artifacts:
    - path: "electron/http/static-server.ts"
      provides: "/api/sessions endpoint with cross-referenced session list"
      contains: "sessionManager.getSessions()"
    - path: "src/src/app/app.component.ts"
      provides: "Polling interval for session list sync"
      contains: "setInterval"
  key_links:
    - from: "src/src/app/app.component.ts"
      to: "/api/sessions"
      via: "fetch polling interval"
      pattern: "fetch.*api/sessions"
    - from: "electron/http/static-server.ts"
      to: "sessionManager.getSessions()"
      via: "HTTP endpoint reads saved sessions"
      pattern: "sessionManager\\.getSessions"
---

<objective>
Fix session list synchronization between Electron window and remote browsers.

**Purpose:** Close UAT Gap 1 (Test 4 failure) — session count mismatch and missing auto-sync for new sessions.

**Output:** Remote browsers show accurate session list that stays synchronized with Electron without requiring manual reload.
</objective>

<execution_context>
@C:/Users/Konstantin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Konstantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-network-access/05-UAT.md
@.planning/phases/05-network-access/05-01-SUMMARY.md
@.planning/phases/05-network-access/05-02-SUMMARY.md

# Relevant source files
@electron/http/static-server.ts
@electron/managers/session-manager.ts
@src/src/app/app.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix /api/sessions endpoint to return saved sessions</name>
  <files>electron/http/static-server.ts</files>
  <action>
Replace the current `/api/sessions` endpoint implementation (which returns raw `ptyManager.getPtyProcesses()`) with a cross-referenced approach:

1. Import `sessionManager` from `../managers/session-manager`
2. Change endpoint to return `sessionManager.getSessions()` instead of raw PTY map
3. Response format should match existing Session interface: `{ id, workingDir, createdAt }[]`
4. Keep existing CORS headers and JSON response structure

**Why this fixes the gap:** The raw PTY map includes stale entries (PTYs that died but weren't cleaned up). sessionManager maintains the canonical session list that's saved to disk and restored on startup.

**Reference:** sessionManager already exists and is wired into main.ts. It has a getSessions() method that returns the saved session list.
  </action>
  <verify>
    <automated>grep -A 5 "GET.*api/sessions" C:/Dev/claude-powerterminal/electron/http/static-server.ts | grep -q "sessionManager.getSessions" && echo "PASS: Endpoint uses sessionManager" || echo "FAIL: Still using raw PTY map"</automated>
    <manual>Check that /api/sessions endpoint reads from sessionManager.getSessions() instead of ptyManager.getPtyProcesses()</manual>
    <sampling_rate>after commit</sampling_rate>
  </verify>
  <done>
`/api/sessions` endpoint returns the saved session list from sessionManager, matching what Electron window shows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add polling interval for remote session list sync</name>
  <files>src/src/app/app.component.ts</files>
  <action>
Add automatic session list refresh for remote browsers:

1. In `ngOnInit()`, after the existing `loadRestoredSessions()` / `loadRemoteSessions()` logic, add a polling interval:
   ```typescript
   // Poll for session updates when in remote browser mode
   if (!window.electronAPI) {
     setInterval(() => {
       this.loadRemoteSessions();
     }, 5000); // 5-second polling
   }
   ```

2. This only runs for remote browsers (when `window.electronAPI` is undefined)
3. Electron window already gets session updates via IPC events, no polling needed there
4. 5-second interval balances responsiveness with network overhead

**Why this fixes the gap:** Remote browsers currently load sessions once on init. This polling keeps the list synchronized when new sessions are created in Electron or other browsers.

**Implementation note:** loadRemoteSessions() already exists and handles the fetch + merge logic. Just wrap it in a timer for remote mode.
  </action>
  <verify>
    <automated>grep -A 3 "!window.electronAPI" C:/Dev/claude-powerterminal/src/src/app/app.component.ts | grep -q "setInterval" && echo "PASS: Polling added" || echo "FAIL: No polling interval"</automated>
    <manual>Verify polling interval is added inside the !window.electronAPI guard and calls loadRemoteSessions()</manual>
    <sampling_rate>after commit</sampling_rate>
  </verify>
  <done>
Remote browsers poll `/api/sessions` every 5 seconds to sync session list automatically.
  </done>
</task>

</tasks>

<verification>
**Manual verification steps:**

1. Start Electron app
2. Create one session in Electron (verify it appears in dashboard)
3. Open phone browser to LAN URL (http://{ip}:9801)
4. Verify phone shows the same session count as Electron
5. Create a new session in Electron
6. Wait 5 seconds, verify the new session appears on phone **without manual reload**
7. Create a session from phone (if remote creation is enabled), verify it syncs to Electron

**Expected behavior:**
- Session counts match exactly
- New sessions appear within 5 seconds across all clients
- No stale sessions from dead PTY processes
</verification>

<success_criteria>
- [ ] `/api/sessions` endpoint uses `sessionManager.getSessions()` instead of raw PTY map
- [ ] Remote browsers poll session list every 5 seconds via `setInterval`
- [ ] Session count matches between Electron and phone browser
- [ ] New sessions created in Electron appear on phone within 5 seconds
- [ ] TypeScript compiles without errors
- [ ] UAT Test 4 passes when re-run
</success_criteria>

<output>
After completion, create `.planning/phases/05-network-access/05-03-SUMMARY.md`
</output>
