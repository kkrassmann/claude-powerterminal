---
phase: 05-network-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/utils/network-info.ts
  - electron/http/static-server.ts
  - electron/websocket/ws-server.ts
  - electron/main.ts
  - src/src/app/app.component.ts
  - src/src/app/app.component.html
autonomous: true
requirements:
  - NET-01
  - NET-03

must_haves:
  truths:
    - "WebSocket server accepts connections from any network interface (0.0.0.0)"
    - "HTTP static server serves Angular build output to remote browsers on port 9801"
    - "App logs its LAN URL to console on startup"
    - "App displays LAN URL in the Electron window header"
    - "electronAPI calls in app.component.ts are guarded for remote browser compatibility"
  artifacts:
    - path: "electron/utils/network-info.ts"
      provides: "LAN IP discovery utility"
      exports: ["getLocalNetworkAddress"]
    - path: "electron/http/static-server.ts"
      provides: "HTTP static file server for Angular build output"
      exports: ["startStaticServer"]
    - path: "electron/websocket/ws-server.ts"
      provides: "WebSocket server bound to 0.0.0.0"
      contains: "host: '0.0.0.0'"
    - path: "electron/main.ts"
      provides: "HTTP server startup, LAN URL console log, IPC for LAN URL"
      contains: "startStaticServer"
  key_links:
    - from: "electron/main.ts"
      to: "electron/http/static-server.ts"
      via: "import and call startStaticServer"
      pattern: "startStaticServer"
    - from: "electron/main.ts"
      to: "electron/utils/network-info.ts"
      via: "import and call getLocalNetworkAddress"
      pattern: "getLocalNetworkAddress"
    - from: "electron/websocket/ws-server.ts"
      to: "0.0.0.0 binding"
      via: "WebSocketServer constructor host option"
      pattern: "host.*0\\.0\\.0\\.0"
---

<objective>
Set up backend network infrastructure for LAN access: bind WebSocket server to 0.0.0.0, create HTTP static server for Angular build output, discover LAN IP, and display the access URL.

Purpose: Enable remote devices on the local network to access the dashboard by making both the WebSocket and HTTP servers listen on all network interfaces, and displaying the LAN URL for easy phone access.

Output: Two new utility files (network-info.ts, static-server.ts), modified ws-server.ts with 0.0.0.0 binding, modified main.ts with HTTP server startup and LAN URL logging, modified app.component with LAN URL display and electronAPI guards.
</objective>

<execution_context>
@C:/Users/Konstantin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Konstantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-network-access/05-RESEARCH.md
@electron/main.ts
@electron/websocket/ws-server.ts
@src/src/app/app.component.ts
@src/src/app/app.component.html
@src/shared/ws-protocol.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create network utilities and HTTP static server</name>
  <files>
    electron/utils/network-info.ts
    electron/http/static-server.ts
  </files>
  <action>
Create two new files:

**electron/utils/network-info.ts:**
- Export `getLocalNetworkAddress(): string | null` function
- Use `os.networkInterfaces()` to iterate all network interfaces
- Return first IPv4 non-internal address found, or null if none
- Import `os` from Node.js built-ins

**electron/http/static-server.ts:**
- Export `startStaticServer(port: number): http.Server` function
- Create HTTP server using `http.createServer`
- Serve files from the Angular build output directory: `path.join(__dirname, '../../src/dist/claude-powerterminal-angular/browser')`
- Implement SPA fallback: requests without file extensions serve index.html
- Handle 404 by serving index.html (Angular routing)
- Define MIME_TYPES map for: .html, .js, .css, .ico, .json, .woff2, .woff, .ttf, .svg, .png, .map
- Bind to `0.0.0.0` on the given port
- Log startup message: `[HTTP] Static server listening on 0.0.0.0:{port}`
- Import `http`, `fs`, `path` from Node.js built-ins

Reference patterns from 05-RESEARCH.md Pattern 2 and Pattern 3. No npm packages needed — all Node.js built-ins.
  </action>
  <verify>
    <automated>npx tsc --noEmit electron/utils/network-info.ts electron/http/static-server.ts 2>&1 || echo "Type check (standalone files may need context — verify no syntax errors manually)"</automated>
    <manual>Verify both files exist with correct exports</manual>
  </verify>
  <done>network-info.ts exports getLocalNetworkAddress, static-server.ts exports startStaticServer, both compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire network infrastructure into app startup and UI</name>
  <files>
    electron/websocket/ws-server.ts
    electron/main.ts
    src/src/app/app.component.ts
    src/src/app/app.component.html
  </files>
  <action>
**electron/websocket/ws-server.ts:**
- Change `new WebSocketServer({ port: WS_PORT })` to `new WebSocketServer({ host: '0.0.0.0', port: WS_PORT })`
- Update console log to: `[WebSocket] Server listening on 0.0.0.0:${WS_PORT}`
- Single-line change, verified via Context7 ws docs.

**electron/main.ts:**
- Import `startStaticServer` from `./http/static-server`
- Import `getLocalNetworkAddress` from `./utils/network-info`
- Import `ipcMain` from electron
- In `app.whenReady().then()`, after `startWebSocketServer()`:
  1. Call `startStaticServer(9801)` to start HTTP server
  2. Call `getLocalNetworkAddress()` to discover LAN IP
  3. Log to console: `\n  LAN access: http://{lanIp}:9801\n` (or "LAN access: not available" if null)
  4. Store the LAN URL in a module-level variable for IPC access
- Register IPC handler: `ipcMain.handle('app:lan-url', () => lanUrl)` where `lanUrl` is the stored URL string or null
- Add `ipcMain` to the electron import at top of file

**src/src/app/app.component.ts:**
- Add a `lanUrl: string | null = null` property
- In the constructor/ngOnInit, check `if (window.electronAPI)` before calling IPC methods
- Guard existing `window.electronAPI.on(IPC_CHANNELS.SESSION_RESTORE_COMPLETE, ...)` with `if (window.electronAPI)`
- Guard existing `window.electronAPI.invoke(IPC_CHANNELS.PTY_LIST)` with `if (window.electronAPI)`
- After guards, fetch LAN URL: `if (window.electronAPI) { this.lanUrl = await window.electronAPI.invoke('app:lan-url'); }`

**src/src/app/app.component.html:**
- Add LAN URL display in the header, between the mute button and session-create:
  `<span class="lan-url" *ngIf="lanUrl">{{ lanUrl }}</span>`
- Style inline or add to app.component.css: small font, muted color (#6c7086), monospace

**IMPORTANT:** Also add 'app:lan-url' to the IPC channels or handle it directly. Since this is a simple one-off, handling directly via `ipcMain.handle` in main.ts and `window.electronAPI.invoke('app:lan-url')` in app.component.ts is simplest. Also ensure the preload script exposes a generic invoke that forwards any channel, or add 'app:lan-url' to the allowed list in preload.ts.

Check `electron/preload.ts` for IPC channel allowlisting — if it uses a whitelist, add the new channel.
  </action>
  <verify>
    <automated>cd /c/Dev/claude-powerterminal && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Start the app and verify console shows LAN URL and WebSocket binds to 0.0.0.0</manual>
  </verify>
  <done>WebSocket server binds to 0.0.0.0, HTTP static server starts on port 9801, LAN URL is logged to console and displayed in app header, electronAPI calls in app.component.ts are guarded</done>
</task>

</tasks>

<verification>
1. `grep -r "host.*0.0.0.0" electron/websocket/ws-server.ts` confirms WS binding
2. `test -f electron/utils/network-info.ts && test -f electron/http/static-server.ts` confirms new files exist
3. `grep "startStaticServer" electron/main.ts` confirms HTTP server is wired in
4. `grep "getLocalNetworkAddress" electron/main.ts` confirms LAN IP discovery is wired in
5. `grep "window.electronAPI" src/src/app/app.component.ts` shows guarded calls (if pattern)
6. `npx tsc --noEmit` passes without errors
</verification>

<success_criteria>
- WebSocket server constructor uses `host: '0.0.0.0'`
- HTTP static server file exists and is started in main.ts on port 9801
- LAN IP is discovered and logged to console on startup
- LAN URL is displayed in the app header UI
- electronAPI calls in app.component.ts are wrapped in existence checks
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-network-access/05-01-SUMMARY.md`
</output>
